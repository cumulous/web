version: 0.1
phases:
  install:
    commands:
      - npm install
  pre_build:
    commands:
      - npm run headless test:once e2e lint
  build:
    commands:
      - npm run build:prod
  post_build:
    commands:
      - aws s3 sync dist/ s3://${WEB_BUCKET}/app/ --exclude index.html